<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_tune"
        android:icon="@drawable/ic_play_circle"
        android:title="@string/tune"
        app:showAsAction="ifRoom"
        android:visible="false"/>

    <item
        android:id="@+id/action_edit"
        android:icon="@drawable/ic_edit"
        android:title="@string/title_edit_hymn"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/actions_hymnals"
        android:icon="@drawable/ic_bookshelf"
        android:title="@string/title_hymnals"
        app:showAsAction="ifRoom" />
</menu>